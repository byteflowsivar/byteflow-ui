# Changelog - Label

## [1.0.0] - 2026-02-28

### Añadido
- Versión inicial del componente `Label`.
- Soporte para el atributo `required` con indicador visual (`*`).
- Soporte para estado `disabled`.
- Estilizado base mediante BEM y variables CSS (`bf-label`).
