# Changelog - Input

## [1.0.0] - 2026-02-28

### Añadido
- Versión inicial del componente `Input`.
- Integración con `@byteflow-ui/label`.
- Soporte para prop `error` con estilos visuales y mensajes.
- Soporte para estados `disabled` y `required`.
- Estilizado base mediante BEM y variables CSS (`bf-input`).
